import { DistancePipePipe } from './distance-pipe.pipe';

describe('DistancePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DistancePipePipe();
    expect(pipe).toBeTruthy();
  });
});
