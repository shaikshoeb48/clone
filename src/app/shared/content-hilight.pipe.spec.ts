import { ContentHilightPipe } from './content-hilight.pipe';

describe('ContentHilightPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentHilightPipe();
    expect(pipe).toBeTruthy();
  });
});
