import { TopmiddleDirDirective } from './topmiddle-dir.directive';

describe('TopmiddleDirDirective', () => {
  it('should create an instance', () => {
  //   const directive = new TopmiddleDirDirective();
  //  expect(directive).toBeTruthy();
  });
});
