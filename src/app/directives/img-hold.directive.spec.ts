import { ImgHoldDirective } from './img-hold.directive';

describe('ImgHoldDirective', () => {
  it('should create an instance', () => {
    // const directive = new ImgHoldDirective();
    // expect(directive).toBeTruthy();
  });
});
