import { MainfeedDirDirective } from './mainfeed-dir.directive';

describe('MainfeedDirDirective', () => {
  it('should create an instance', () => {
    // const directive = new MainfeedDirDirective();
    // expect(directive).toBeTruthy();
  });
});
