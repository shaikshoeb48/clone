import { TopbarDirDirective } from './topbar-dir.directive';

describe('TopbarDirDirective', () => {
  it('should create an instance', () => {
    // const directive = new TopbarDirDirective();
    // expect(directive).toBeTruthy();
  });
});
