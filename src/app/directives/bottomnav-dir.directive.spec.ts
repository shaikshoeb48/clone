import { BottomnavDirDirective } from './bottomnav-dir.directive';

describe('BottomnavDirDirective', () => {
  it('should create an instance', () => {
    // const directive = new BottomnavDirDirective();
    // expect(directive).toBeTruthy();
  });
});
